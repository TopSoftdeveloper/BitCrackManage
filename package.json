{
  "name": "cubitcrack-manager",
  "version": "2.0.0",
  "description": "Multi-instance Node.js manager for cuBitCrack: detects NVIDIA GPUs, runs one instance per GPU (each with its own workspace + key range), merges finds into btc_found.txt and reports to Discord.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "keywords": [
    "bitcrack",
    "bitcoin",
    "cuda",
    "manager"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  }
}

